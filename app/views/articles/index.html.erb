<div class="container">
		
	<h1>Articles Index Page</h1>
	<table  class="table table-bordered">
		<thead>
			<tr>
				<th>Title</th>
				<th>Description</th>
				<th colspan="3">Actions</th>
			</tr>
		</thead>
		<tbody>
			<% @articles.each do |article| %>
			<tr>
				<td><%= article.title%></a></td>
				<td><%= article.description%></td>
				<td id="buttons">
					<center>
						
					<%=  link_to 'Show', article_path(article) , :class=> "btn btn-primary"%>
					<%= link_to 'Edit', edit_article_path(article) , :class=> "btn btn-primary"%>
					<%= link_to 'Delete', article_path(article), method: :delete, data: { Confirm: "Sure want to delete?"} , :class=> "btn btn-primary" %>
					</center>
				</td>
			</tr>
			<%end%>
		</tbody>
	</table>
	<br><br>
	<%= link_to 'Create New Article', new_article_path , :class=> "btn btn-primary"%>
	<%= link_to 'Home Page', root_path , :class=> "btn btn-primary"%>
	<br><br>
	</div>